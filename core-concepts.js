// variable: let, const
let money = 50;
money =40;
const name ='Hasib';

let isGoing = true;

 const person = {
     name: 'Surovi',
     age: 22,
     city: 'Barishal'
 }
 const statement = `This person ${name} Love ${person.name}`;
 console.log(statement);

 //condition
 